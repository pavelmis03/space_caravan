image: "python:3.7"

stages:
  - setup
  - test

py-pip:
  stage: setup
  script:
    - pip install -r requirements.txt
    - pip install pylint

pylint:
  stage: test
  script:
    - ci/pylint_runner.sh